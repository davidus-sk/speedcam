<?php

// get posted data
$camera = $_POST['camera'];
$speed = $_POST['speed'];
$ts = $_POST['ts'];
$radar = $_POST['radar'];
$plate = $_FILES['plate'];
$car = $_FILES['car'];

// save data
if (!empty($camera) && !empty($speed) && !empty($ts) && !empty($radar)) {
  
}//if

echo "ERROR";
